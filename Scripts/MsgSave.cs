using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MsgSave : MonoBehaviour
{
    public InputField m_input;
    public Text m_text;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if(m_text.text == "save!"){
        //     Debug.Log("Oh Yeah!");
        //     Destroy(this.gameObject);
        // }
    }
    public void MsgIn(){
        // Debug.Log("test");
        // Debug.Log(m_input.text);
        // if(m_input.Placeholder.activeSelf == true){
        //     Debug.Log("is Active");
        //     m_input.SetActive(false);
        // }
    }
}
